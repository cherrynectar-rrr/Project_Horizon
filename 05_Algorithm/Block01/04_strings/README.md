# Strings — 字符串

[← 返回 Block 01](../README.md)

## 1. 本课概览

字符串可以先把它理解成：

> **一串按顺序排列的字符。**

因此很多数组思维可以直接迁移过来：按下标访问、遍历、从两端向中间移动。

本课真实练习覆盖过：

- 已知下标访问字符；
- 遍历整个字符串；
- 统计某个字符出现次数；
- 原地反转字符串；
- 判断一个字符串是否为回文。

当前 `main.cpp` 最终运行的是回文判断。

---

## 2. 直觉 / Intuition

假设：

```text
s = "level"
```

如果要判断它是不是回文，最直接的观察是：

```text
第一个字符 == 最后一个字符
第二个字符 == 倒数第二个字符
...
```

我们没有必要把字符串重新复制一份再反转后比较。

只要从两端向中间检查即可。

这和上一课的数组原地反转很像：

> 左边位置和右边位置之间存在明确的对应关系。

---

## 3. 核心思想 / Core Idea

### 下标访问

```cpp
s[i]
```

已知位置时可以直接访问，因此通常看作 `O(1)`。

### 遍历与统计

如果要打印所有字符，或者统计某个字符出现了多少次，最坏都需要看完整个字符串：

```text
O(n)
```

### 回文判断

对于长度为 `n` 的字符串，只需要比较前后对应字符：

```text
i
n - 1 - i
```

最多检查一半位置。

如果发现任意一对不相等，可以立刻返回 `false`。

如果所有对应位置都相等，则返回 `true`。

---

## 4. 手推 / Hand Simulation

以：

```text
s = "radar"
```

为例。

第一次：

```text
s[0] = 'r'
s[4] = 'r'
相等，继续
```

第二次：

```text
s[1] = 'a'
s[3] = 'a'
相等，继续
```

中间字符：

```text
s[2] = 'd'
```

不需要和自己比较。

因此只需检查到：

```text
s.size() / 2
```

即可。

再看：

```text
s = "hello"
```

第一对：

```text
'h' != 'o'
```

马上就能返回 `false`，不需要继续检查剩余字符。

---

## 5. 实现 / Implementation

本课真实代码：[`main.cpp`](./main.cpp)。

当前核心实现：

```cpp
bool isPalindrome(const std::string& s)
{
    for (std::size_t i = 0; i < s.size() / 2; i++)
    {
        if (s[i] != s[s.size() - 1 - i])
        {
            return false;
        }
    }
    return true;
}
```

几个重要点：

- `const std::string&`：只读原字符串，不复制；
- `i < s.size() / 2`：只需要检查一半；
- 一旦发现不匹配，立即返回 `false`；
- 所有对应字符都通过后才返回 `true`。

同一文件中还保留了之前的：

- `printString`；
- `countChar`；
- `reverseString`。

它们共同记录了从基础遍历逐渐走向“利用两端对应关系”的学习过程。

---

## 6. 复杂度 / Complexity

| 操作 | 时间复杂度 | 辅助空间 |
| --- | --- | --- |
| 已知下标访问字符 | `O(1)` | `O(1)` |
| 遍历字符串 | `O(n)` | `O(1)` |
| 统计字符次数 | `O(n)` | `O(1)` |
| 原地反转 | `O(n)` | `O(1)` |
| 回文判断（最坏） | `O(n)` | `O(1)` |

回文判断虽然最多比较约 `n/2` 次，但忽略常数后仍然是 `O(n)`。

如果很早就发现不匹配，实际运行会更快；不过最坏情况下仍需检查到中间。

---

## 7. 常见错误 / Common Mistakes

### 右侧下标写成 `size - i`

最后一个合法下标是：

```text
size - 1
```

因此对应位置是：

```text
size - 1 - i
```

### 为判断回文额外创建反转副本

这样也能判断，但会产生 `O(n)` 额外空间。当前实现可以直接只读原字符串完成判断。

### 忘记提前返回

一旦发现任意对应字符不同，已经可以确定不是回文，不需要继续扫描。

### 把“检查一半”误认为复杂度是 `O(n/2)`

大 O 忽略常数因子，所以仍记作 `O(n)`。

---

## 8. 精选例题 / Selected Problems

暂未额外收录。

当前回文判断已经是一个很好的基础模式：从两端向中间比较。以后学习 Two Pointers 时会再次看到类似的移动思想。

---

## 9. 快速复习 / Quick Review

先闭卷回答：

1. 为什么字符串可以像数组一样按下标访问？
2. 统计某字符出现次数为什么最坏需要 `O(n)`？
3. 回文判断为什么只需要检查一半？
4. `s.size() - 1 - i` 表示什么？
5. 当前回文实现为什么是 `O(1)` 辅助空间？

### 一句话唤醒

> **字符串是字符序列；遍历需要线性时间，而前后对应关系可以让我们直接从两端检查。**

---

## 10. Knowledge Connections

**Before:** [Arrays — 数组](../03_arrays/)  
**Now:** 把数组下标与两端对应思想迁移到字符序列  
**Later:** [Two Pointers — 双指针](../07_two_pointers/) 会把“两端移动”的思路变成更明确的算法模式。
